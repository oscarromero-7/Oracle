SELECT LPAD(' ', 6* (LEVEL-1)) || TO_CHAR(E.CEDULA) CEDULA,
	E.NOMBRE,
	E.APELLIDO
FROM B_EMPLEADOS E
CONNECT BY E.CEDULA = PRIOR E.CEDULA_JEFE
START WITH UPPER(E.NOMBRE) LIKE 'GLORIA%' AND UPPER(E.APELLIDO) LIKE 'MENDOZA%'

/
