User
DELETE FROM B_CATEGORIAS_SALARIALES
WHERE COD_CATEGORIAS_SALARIALES =( SELECT CS.COD_CATEGORIA
				   FROM B_POSICION_ACTUAL PA
				   RIGHT JOIN B_CATEGORIAS_SALARIALES CS ON 						   						   PA.COD_CATEGORIA = CS.COD_CATEGORIA
				   WHERE PA.CEDULA IS NULL
				   ORDER BY 1)
/
